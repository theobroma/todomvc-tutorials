{
    "name": "todomvc-tutorials",
    "private": false,
    "version": "0.0.1",
    "license": "ISC",
    "type": "module",
    "scripts": {
        "build": "cd vite-reactts-19version && npm run build",
        "serve": "cd vite-reactts-19version && serve -s dist",
        "predeploy": "cd vite-reactts-19version && npm run build",
        "deploy": "gh-pages -d vite-reactts-19version/dist"
    },
    "dependencies": {},
    "devDependencies": {
        "gh-pages": "^6.3.0"
    },
    "engines": {
        "node": "^24.x"
    }
}
